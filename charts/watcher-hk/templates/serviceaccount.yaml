{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "watcherhk.serviceAccountName" . }}
  labels:
    {{- include "watcherhk.labels" . | nindent 4 }}
{{- end }}
